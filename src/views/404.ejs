<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Dental Katalog</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <%- include('partials/header', { filters: {} }) %>

    <div class="container">
        <div class="error-page">
            <h1>404 - <%= title %></h1>
            <p class="error-message"><%= message %></p>

            <% if (typeof sku !== 'undefined') { %>
                <p class="error-detail">SKU: <code><%= sku %></code></p>
            <% } %>

            <div class="error-actions">
                <a href="/products" class="btn btn-primary">Alle Produkte durchsuchen</a>
                <a href="/" class="btn btn-secondary">Zur Startseite</a>
            </div>

            <div class="error-suggestions">
                <h2>Vorschläge:</h2>
                <ul>
                    <li>Verwenden Sie die Suchfunktion, um nach ähnlichen Produkten zu suchen</li>
                    <li>Stöbern Sie durch unsere Herstellerkategorien</li>
                    <li>Kehren Sie zur Übersicht zurück und filtern Sie nach Ihren Bedürfnissen</li>
                </ul>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html>
